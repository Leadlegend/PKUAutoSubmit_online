name: PKUAutoSubmit

on:
  schedule:
    - cron: '0 4 * * *'
    
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v2
        
      - name: 'Set up Python'
        uses: actions/setup-python@v1
        with:
           python-version: 3.7
           
      - name: 'Install requirements'
        run: 
          pip install selenium
        
      - name: 'Working'
        run:
          python main.py --ID ${{secrets.ID}} --ID2 ${{secrets.ID2}} --PASSWORD ${{secrets.PASSWORD}} --PASSWORD2 ${{secrets.PASSWORD2}} --MAIL_ADDRESS ${{secrets.MAIL_ADDRESS}} --PHONE_NUMBER ${{secrets.PHONE_NUMBER}} --PHONE_NUMBER2 ${{secrets.PHONE_NUMBER2}} --SENDKEY ${{secrets.SENDKEY}}
          

